# Phase 1: Docker Basics

**Status:** almost done
**Dates:** 
july 31 2026 done still docker ps and docker ps -a

## Topics to cover
- [ ] Containers vs Virtual Machines
- [ ] What is a Docker image vs a container
- [ ] Docker architecture (Engine, Daemon, CLI)
- [ ] Core commands: `docker run`, `ps`, `stop`, `rm`, `images`, `pull`, `exec -it`, `logs`

## Things to search
- "docker vs virtual machine"
- "docker architecture explained"
- "docker run vs docker start difference"
- "docker ps vs docker ps -a"
- "what is a docker image layer"

## My notes
<!-- https://medium.com/@ravipatel.it/understanding-virtual-machines-vs-docker-containers-a-technical-comparison-241f370b2076 -->
Containers vs Virtual Machines :-
    docker container are light weight container that directly talk to host os kernel making the docker to start fast.
    virtual machines are the virtual machine we can create using vmWare and any other tool that has there own kernel os ram and vm run on host os making double layer first command talk to vm kernel then vm kernal talk to originla host making application to start slow.

docker architecture explained :-
docker deamon :- 
    docker deamon is responsible for managing docker images,container,volumes.

docker client - 
    is a command like tool using which we can manage images,container,volumes with commands.

docker images -
     docker images is static,read only templates it only consumes disk size,contain the application code, runtime, system tools, libraries, and other dependencies. Docker images are created from Dockerfiles.

docker containers :- 
    Docker container is a live, running instance which are dynamic they consume ram,cpu usage etc. They encapsulate the application and its dependencies, providing an isolated environment for execution.

Docker Registry:

    Docker Registry is a centralized repository for storing and sharing Docker images. The default public registry is Docker Hub, where users can find a vast collection of images. Organizations can also set up private registries to store proprietary images securely.

Docker Compose:

    Docker Compose is a tool for defining and running multi-container Docker applications. It uses a YAML file (docker-compose.yml) to specify services, networks, volumes, and other configurations required for the application. Docker Compose simplifies the management of complex applications composed of multiple interconnected containers.

Docker Volumes:

    Docker volumes are used for persisting data generated by and used by Docker containers. They provide a way for containers to store and share data independently of the container lifecycle, ensuring data persistence and portability.

Docker Networking:

    Docker provides networking capabilities for containers to communicate with each other and with external networks. It uses software-defined networks (SDN) to create virtual networks, enabling connectivity and isolation. Users can create custom networks, connect containers to networks, and define network policies using Docker commands or Docker Compose.

docker run vs docker start difference:-

docker run :- 
    docker run is used to strat the brand new container. docker run -d --name <conatiner name>
    here -d refers to -d: run the container in the background and print the new container ID
    --name: assign the name container-name to the container

docker start :-
        The docker start command is used to start an existing, stopped container.
        docker start <container name>


docker ps vs docker ps -a :
    docker ps show currnt running container and docker ps -a shows all the running stopped container.

dcoker image layer :
    dcoker image layers are basically the command we write in docker file each command represet the layer which excute on top of each other. trying to multiple command  in single line reduct image size and build time. 

-

## Things that confused me / took time to click
-

## Commands I practiced
```bash
# paste commands you ran and what they did
```

## Practice log
- [X] Pulled and ran `hello-world`
<!-- https://hub.docker.com/search -->
- [X] Pulled and ran `nginx`, accessed it in browser
<!-- https://hub.docker.com/_/mysql -->
- [X] Pulled and ran `mysql`, connected to it
- [X] Practiced starting/stopping/removing containers :- Start,Stop,ps,ps -a,rm
